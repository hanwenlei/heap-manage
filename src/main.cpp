#include"Test.h"

int main(void)
{
//	clock_t start = clock();
	MyAllocate();
//	clock_t finish = clock();
//	double DuringTime = (finish - start) / CLOCKS_PER_SEC;
//	printf("DuringTime : %lf seconds\n", DuringTime);
	getchar();
	return 0;
}
